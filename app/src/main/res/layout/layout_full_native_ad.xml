<?xml version="1.0" encoding="utf-8"?>
<FrameLayout android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:id="@+id/ad_root"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

        <FrameLayout
            android:id="@+id/adFrame"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />


        <com.facebook.shimmer.ShimmerFrameLayout
            android:id="@+id/shimmerFbAd"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone"
            app:shimmer_duration="1000">

            <include layout="@layout/shimmer_design_full_native" />

        </com.facebook.shimmer.ShimmerFrameLayout>

        <LinearLayout
            android:id="@+id/fallbackContainer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="24dp"
            android:visibility="gone">

            <ImageView
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:src="@drawable/ic_alarm"
                app:tint="@color/ad_body_color" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:text="@string/unable_to_load_ad"
                android:textColor="@color/ad_body_color"
                android:textSize="16sp"
                android:fontFamily="@font/poppins_semi_bold" />

            <TextView
                android:id="@+id/btnContinue"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="20dp"
                android:background="@drawable/bg_extra_less_round_accent"
                android:gravity="center"
                android:padding="10dp"
                android:text="@string/continuee"
                android:visibility="gone"
                android:textColor="@color/white"
                android:textSize="18sp"
                android:fontFamily="@font/poppins_semi_bold" />
        </LinearLayout>

</FrameLayout>
