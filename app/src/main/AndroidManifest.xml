<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Only permissions this base project actually uses. Each fork must add back what it
      needs and nothing more — every declared permission it does not use is a Play data
      safety declaration it has to justify, and location / READ_MEDIA_IMAGES are in the
      sensitive tier.

      Removed from the base (0 call sites, no corresponding API usage anywhere):
        ACCESS_FINE_LOCATION, ACCESS_COARSE_LOCATION,
        READ_EXTERNAL_STORAGE, WRITE_EXTERNAL_STORAGE, READ_MEDIA_IMAGES
    -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />


    <!--
      usesCleartextTraffic was removed: it allowed plain HTTP app-wide with no
      network-security-config. If a fork genuinely needs HTTP to a specific host, add a
      networkSecurityConfig with a domain allowlist rather than opening everything.
    -->
    <application
        android:name=".app.MyApp"
        android:allowBackup="true"
        android:allowClearUserData="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App"
        android:localeConfig="@xml/locales_config"
        tools:replace="android:fullBackupContent"
        tools:targetApi="31">

        <activity
            android:name=".ui.screens.PremiumActivity"
            android:exported="false"
            />

        <!-- exported=false: no intent-filter, only ever launched from within the app. -->
        <activity
            android:name=".ui.screens.MainActivity"
            android:exported="false"/>

        <!--
          No screenOrientation lock, deliberately.

          For apps targeting API 36+ (this one targets 37), Android ignores activity
          orientation and resizability restrictions on large screens (smallest width
          >= 600dp). A "portrait" lock therefore does nothing on tablets and unfolded
          foldables, so relying on it would mean onboarding renders in landscape there
          anyway — just untested. Lint also flags it (LockedOrientationActivity).

          It is not needed regardless: activity_onboarding gives the pager 0dp height
          constrained above a wrap_content bottom bar, and item_onboarding puts the
          illustration in a 0dp/weight=1 slot, so the layout absorbs a short viewport.
        -->
        <activity
            android:name=".ui.screens.OnboardingActivity"
            android:exported="false" />
        <activity
            android:name=".ui.screens.SurveyActivity"
            android:exported="false"
             />
        <activity
            android:name=".ui.screens.LanguageActivity"
            android:exported="false"
            />
        <activity
            android:name=".ui.screens.UninstallActivity"
            android:exported="true"
             />
        <activity
            android:name=".ui.screens.StartActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>


        <meta-data
            android:name="com.google.android.gms.ads.flag.OPTIMIZE_INITIALIZATION"
            android:value="true" />
        <meta-data
            android:name="com.google.android.gms.ads.flag.OPTIMIZE_AD_LOADING"
            android:value="true" />
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/admob_app_id" />

        <!-- Meta SDK -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="@string/facebook_client_token" />

    </application>

</manifest>